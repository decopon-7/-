# ほいくつなぐ 開発メモ（Claude Code 用）

このリポジトリは公開されている。勤務先の園名・個人の情報・価格や事業の内容は書かないこと。

## 今回のスコープ

アプリ開発に集中する。集客・販売まわりは触らない。

## 依頼者について

- 現役保育士（0〜2歳の乳児保育が専門）。園の管理業務も担当
- 割ける時間は週5〜10時間。プログラミング専業ではなく「現場で使えるか」で判断する
- **推測を結論の決め手にしない。** 判断材料にはしてよいが断定の根拠にはしない
- 不明点は憶測で埋めず、公式に照会してから進める

## 開発対象：ほいくつなぐ

保育施設向けICTアプリ。出欠連絡・連絡帳・午睡チェックを1つの園アプリにまとめる。

- 保育士と保護者の双方が使う。**アプリは保育士用と保護者用に分け、まず保育士用を完成させる**
- 進め方は「設計をまとめてから、そのまま試作する」

### 現状（2026-09-25）

- 作り方は **コードのPWA ＋ Cloudflare Workers ＋ D1** に決定
- 保育士用の **午睡チェック v0.3** を作成済み
  - 管理者が端末を登録し、職員は名前を選ぶだけ。午睡担当2名（①②）のどちらが入力したかを記録
  - 体位（矢印）・室温・湿度。確認間隔は0歳5分・それ以外10分（依頼者の決定）
  - 園ごとのデータの分離、複数タブレットでの共有、オフライン時の送信待ち、設定は管理者モード（10分）のみ
  - 設計：`docs/gosui-check-design.md`、`docs/server-design.md`
- 給食の **未経験食材チェック v0.1** を作成済み（依頼者の決定：他社に見当たらない機能として着手）
  - 「給食」タブ：配膳前チェック（献立×子どもごとの食べた食材を自動照合し、未経験・除去がある子だけ表示、2名で確認して記録）／今日の献立／食べた食材（未・家庭で食べた・除去をタップで切替）
  - 設計：`docs/meal-check-design.md`
  - 献立の食材は「1つずつ足す／外す」操作（丸ごと置き換えではない）。連打で前の選択が消える不具合をブラウザで再現して直した経緯あり。詳細は設計メモの「決めたこと」参照
- `npm test` で判定ロジック・APIをテストする（ログイン・権限・園の分離・給食の照合など、計53件）
- Cloudflare への公開はまだ（依頼者の Cloudflare アカウントでの作業が必要。手順は README）
- 出欠連絡・連絡帳は未着手

### コードの約束

- 画面は素の HTML / CSS / JavaScript（ビルドなし）。判定ロジックは画面から切り離して `app/logic.js`・`app/ops.js` に置き、テストする
- APIでは必ず登録した端末（セッション）の `facility_id` で絞り込む。新しいAPIを足したら、別の園から触れないことをテストに加える
- 園児の情報を含む `/api/` の応答はサービスワーカーでキャッシュしない
- CSP で inline の style / script を禁止している。HTML文字列に `style=""` を書かない

## 既存アプリ（流用できる資産）

- **水遊びチェック表**：タブレット・スマホ向けPWA、Cloudflareで配信、現場で使用中。タップだけで入力・修正が完結し、参加できる子・できない子が一覧でわかる。午睡チェックの下敷き
- **新人職員 自己評価チェックリスト**：88項目・本人／先輩／主任の3者を0〜4の5段階で評価。タブ切り替え、「結果」タブは3者を並べて表示（編集不可）、アコーディオンで大項目を開閉

### 踏襲するUI方針

- タップだけで入力が完結する（キーボード入力を極力減らす）
- 一覧性を最優先（誰が該当・非該当かが一目でわかる）
- 初めて使うスタッフでも迷わない導線
- 配色はディープグリーン系＋淡いグレー背景（`app/style.css` の `:root` にまとめてある）

### UI/UXを1番の軸にする（依頼者の決定・2026-09-25）

新しい画面を作る時・既存画面を直す時は、機能追加より先にこの3つを見る。

- **使い勝手**：ボタンの押しやすさ（タップ範囲・押した時の反応）、読み込み中／失敗時の表示、余白や文字サイズの一貫性
- **見た目**：既存の水遊びチェック表と同じ配色（ディープグリーン＋淡いグレー）は変えない（現場で使っている別アプリとの統一感のため）。そのうえで、影・角丸・間隔・アイコン・アニメーションを整えて洗練させる（`app/style.css` の `:root` のトークンを起点にする）
- **導線**：迷わず使えることを最優先。タブにはアイコンをつける（`app/index.html` のタブ参照）。新しい画面・機能を作る時も、迷わせないことを最初に設計する

## 要件定義の材料

コドモン・ルクミー・はいチーズ！の比較用質問票（重視5項目：午睡・安全／連絡帳／食事・水分・排泄・健康／保育記録・個別記録／先生の使いやすさ）が、そのまま要件のチェックリストになる。監査が多い施設形態のため、記録を紙に出せること・記録者が残ることを重視する。

## 技術・セキュリティの前提

- 園児の情報を扱うため、**認証・権限設計は最初から入れる。後付けにしない**（試作版はサンプルデータのみで運用し、実データを入れる前に必ず入れる）

## 表記ルール（ドキュメント・UI文言）

- ハートの絵文字は使わない
- 「元保育士」ではなく「現役保育士」
- 一人称は「私」ではなく「自分」
- 温かみのあるホスピタリティ寄りの日本語

## 次にやること

1. Cloudflare に公開し、現場のタブレットでサンプルの子どもを使って試してもらう
2. `docs/gosui-check-design.md` の「現場で確かめたいこと」を依頼者に確認する
3. 未経験食材チェックが、コドモン・ルクミー・はいチーズ！に本当に無い機能か、各社へ確認する（`docs/meal-check-design.md` 参照。公式サイトで確認した範囲で見当たらなかっただけで断定はしていない）
4. 記録の保存期間・書き出し・バックアップの方針を決める（監査で求められる期間を公式に確認）
5. 次の機能（出欠連絡 / 連絡帳）の設計
